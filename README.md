# jaac

**Java-As-A-Compiler** — write and run Java code entirely in your browser using CheerpJ. No server-side compilation needed.

## Run with Docker

```sh
docker build -t jaac .
docker run -d -p 8080:80 jaac
```

Open `http://localhost:8080`.
